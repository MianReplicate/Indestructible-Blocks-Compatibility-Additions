modLoader = "javafml"
loaderVersion = "${forge_loader_version_range}"
license = "${mod_license}"
issueTrackerURL = "${issue_tracker_url}"

[[mods]]
modId = "indestructible_blocks_compat"
version = "${mod_version}"
displayName = "${mod_name}"
logoFile = "${logo_file}"
credits="${mod_credits}"
authors = "${mod_authors}"

description = "${mod_description}"

#Neoforge dependency
[[dependencies.indestructible_blocks_compat]]
modId = "forge"
mandatory = true
versionRange = "${forge_version_range}"
ordering = "NONE"
side = "BOTH"

#Minecraft dependency
[[dependencies.indestructible_blocks_compat]]
modId = "minecraft"
mandatory = true
versionRange = "[${minecraft_version},)"
ordering = "NONE"
side = "BOTH"

[[dependencies.indestructible_blocks_compat]]
modId = "indestructible_blocks"
mandatory = true
side = "BOTH"
ordering = "AFTER"
versionRange = "[${indestructible_blocks_version},)"